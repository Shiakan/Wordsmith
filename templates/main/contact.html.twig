{% extends 'base.html.twig' %}
{% block main %}
    <div class='wrapper'>
        <div class='container'>
            <h1>Contacté nous</h1>
            {{form_start(form)}}
            {{form_widget(form)}}
            <button class="btn btn-success">{{ button_label|default('Envoyer') }}</button>
            {{form_end(form)}}
        </div>
    </div>
{% endblock %}