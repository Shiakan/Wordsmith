{% extends 'base.html.twig' %}

{% block title %}Edition du Thread{% endblock %}

{% block main %}
    <h1>Modificatiion du thread {{thread.title}}</h1>
    {{ form_start(editForm) }}
    {{form_widget(editForm)}}
    <button class="btn">{{ button_label|default('Modifié') }}</button>
    {{form_end(editForm)}}


    <a href="{{ path('thread_index') }}">back to list</a>
{% endblock %}
