<form method="post" action="{{ path('subcategory_delete', {'id': subcategory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');"class="btn-delete">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subcategory.id) }}">
    <button title="Supprimer la sous-catégorie" class="btn btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
